'use client';
import{useEffect,useState}from'react';
import{Bell,BellRing}from'lucide-react';
import{createBrowserClient}from'@supabase/ssr';
function b64(s:string){const p='='.repeat((4-s.length%4)%4),b=(s+p).replace(/-/g,'+').replace(/_/g,'/'),r=atob(b);return Uint8Array.from([...r].map(c=>c.charCodeAt(0)))}
export default function PushNotifications({clientId}:{clientId:string}){const[state,setState]=useState<'idle'|'on'|'blocked'|'loading'>('idle');useEffect(()=>{if(typeof Notification!=='undefined'){setState(Notification.permission==='granted'?'on':Notification.permission==='denied'?'blocked':'idle')}},[]);async function enable(){try{setState('loading');const permission=await Notification.requestPermission();if(permission!=='granted'){setState(permission==='denied'?'blocked':'idle');return}const reg=await navigator.serviceWorker.ready;const key=process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY;if(!key)throw new Error('Chave de notificações ainda não configurada.');let sub=await reg.pushManager.getSubscription();if(!sub)sub=await reg.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:b64(key)});const json=sub.toJSON(),supabase=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);const{error}=await supabase.from('push_subscriptions').upsert({client_id:clientId,endpoint:json.endpoint,p256dh:json.keys?.p256dh,auth:json.keys?.auth,user_agent:navigator.userAgent,active:true,updated_at:new Date().toISOString()},{onConflict:'endpoint'});if(error)throw error;setState('on')}catch(e){setState('idle');alert(e instanceof Error?e.message:'Não foi possível ativar as notificações.')}}if(state==='on')return <div className="portal-highlight"><BellRing size={22}/><div><span>Notificações</span><strong>Ativadas neste celular</strong><small>Você receberá lembretes das suas parcelas.</small></div></div>;return <div className="portal-highlight"><Bell size={22}/><div><span>Lembretes de parcelas</span><strong>{state==='blocked'?'Notificações bloqueadas':'Ative as notificações'}</strong><small>{state==='blocked'?'Libere as notificações nas configurações do navegador.':'Receba aviso 2 dias antes e no vencimento.'}</small>{state!=='blocked'&&<button type="button" className="primary" onClick={enable} disabled={state==='loading'}>{state==='loading'?'Ativando...':'Ativar notificações'}</button>}</div></div>}
