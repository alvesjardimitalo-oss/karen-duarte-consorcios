'use client';
import { useState } from 'react';
import { Copy, QrCode, X } from 'lucide-react';
export function PixPayment({pixKey}:{pixKey:string}){const[copied,setCopied]=useState(false),[showQr,setShowQr]=useState(false);async function copy(){await navigator.clipboard.writeText(pixKey);setCopied(true);setTimeout(()=>setCopied(false),1800)}return <div className="pix-actions"><button type="button" onClick={copy} className="pix-copy"><Copy size={16}/>{copied?'PIX copiado!':'Copiar PIX'}</button><button type="button" onClick={()=>setShowQr(true)} className="pix-qr"><QrCode size={16}/>QR Code</button>{showQr&&<div className="pix-modal" onClick={()=>setShowQr(false)}><div className="pix-modal-card" onClick={e=>e.stopPropagation()}><button className="pix-close" onClick={()=>setShowQr(false)}><X/></button><p className="eyebrow">PAGAMENTO VIA PIX</p><h2>Escaneie o QR Code</h2><div className="pix-qr-placeholder"><QrCode size={110}/><span>Chave PIX</span><strong>{pixKey}</strong></div><p className="muted">QR dinâmico com valor da parcela será conectado na próxima etapa.</p><button className="primary" onClick={copy}><Copy size={16}/>{copied?'PIX copiado!':'Copiar chave PIX'}</button></div></div>}</div>}
