import Link from 'next/link';
import { Gift, LayoutDashboard, ReceiptText, ShoppingBag, UserRound, Users } from 'lucide-react';
type Profile={name:string;role:string;avatar_url:string|null};
export function StaffNav({profile,active}:{profile:Profile;active?:string}){const items=[['dashboard','/','Dashboard',LayoutDashboard],['consorcios','/consorcios','Consórcios',Gift],['clientes','/clientes','Clientes',Users],['parcelas','/parcelas','A receber',ReceiptText],['pedidos','/pedidos','Vendas',ShoppingBag]] as const;return <aside className="app-nav"><Link href="/" className="app-nav-brand"><span>KM</span><div><strong>Karen Martins</strong><small>COSMÉTICOS · CONSÓRCIOS</small></div></Link><nav>{items.map(([key,href,label,Icon])=><Link href={href} className={active===key?'active':''} key={key}><Icon size={19}/><span>{label}</span></Link>)}</nav><Link href="/perfil" className="app-nav-profile">{profile.avatar_url?<img src={profile.avatar_url} alt=""/>:<i><UserRound size={19}/></i>}<div><small>Meu perfil</small><strong>{profile.name}</strong><span>{profile.role}</span></div></Link></aside>}
