'use client';import{Image as ImageIcon,Printer}from'lucide-react';
export function PurchaseReceiptActions({targetId,fileName='comprovante'}:{targetId:string;fileName?:string}){function print(){const el=document.getElementById(targetId);if(!el)return;const w=window.open('','_blank','width=460,height=760');if(!w)return;const css='*{box-sizing:border-box}body{margin:0;background:#eee;color:#111;font-family:ui-monospace,SFMono-Regular,Menlo,monospace}.paper{width:80mm;margin:12px auto;padding:7mm 5mm;background:#fff;font-size:11px}.receipt-brand{text-align:center;display:grid;gap:2px;margin:8px 0 14px}.receipt-brand strong{font-size:18px}.receipt-brand span,.receipt-brand small{font-size:9px}.receipt-divider{border-top:1px dashed #777;margin:12px 0}.receipt-line,.receipt-total,.receipt-item>div,.receipt-installment{display:flex;justify-content:space-between;gap:12px}.receipt-line{padding:3px 0;font-size:10px}.receipt-line b{text-align:right}.receipt-item{padding:6px 0}.receipt-item>strong{display:block;font-size:10px}.receipt-item>small,.receipt-installment small{display:block;font-size:8px;color:#555}.receipt-total{align-items:center;font-size:12px}.receipt-total strong{font-size:16px}.receipt-installment{padding:6px 0;border-bottom:1px dotted #ddd;font-size:9px}.receipt-installment>div:last-child{text-align:right}.receipt-pending,.receipt-paid{font-weight:700;border-top:1px dotted #999;margin-top:4px;padding-top:6px}.receipt-actions{display:none!important}@page{size:80mm auto;margin:0}@media print{body{background:#fff}.paper{margin:0;width:80mm;padding:5mm;box-shadow:none}}';w.document.write('<!doctype html><html><head><meta charset="utf-8"><title>'+fileName+'</title><style>'+css+'</style></head><body><div class="paper">'+el.innerHTML+'</div></body></html>');w.document.close();w.focus();setTimeout(()=>w.print(),250)}
function image(){const el=document.getElementById(targetId);if(!el)return;const lines=(el.innerText||'').trim().split('\n').map(x=>x.trim()).filter(Boolean),canvas=document.createElement('canvas'),ctx=canvas.getContext('2d');if(!ctx)return;const width=760,pad=46,line=30,maxChars=58,wrapped:string[]=[];for(const l of lines){if(l.length<=maxChars){wrapped.push(l);continue}let rest=l;while(rest.length>maxChars){let cut=rest.lastIndexOf(' ',maxChars);if(cut<20)cut=maxChars;wrapped.push(rest.slice(0,cut).trim());rest=rest.slice(cut).trim()}if(rest)wrapped.push(rest)}const height=Math.max(500,pad*2+wrapped.length*line+20);canvas.width=width;canvas.height=height;ctx.fillStyle='#fff';ctx.fillRect(0,0,width,height);ctx.fillStyle='#111';ctx.font='18px monospace';let y=pad;for(const l of wrapped){ctx.fillText(l,pad,y);y+=line}ctx.strokeStyle='#bbb';ctx.beginPath();ctx.moveTo(pad,y+4);ctx.lineTo(width-pad,y+4);ctx.stroke();const a=document.createElement('a');a.download=fileName+'.png';a.href=canvas.toDataURL('image/png');a.click()}
return <div className="receipt-actions"><button type="button" className="secondary" onClick={print}><Printer size={15}/>Imprimir / PDF</button><button type="button" className="secondary" onClick={image}><ImageIcon size={15}/>Salvar imagem</button></div>}