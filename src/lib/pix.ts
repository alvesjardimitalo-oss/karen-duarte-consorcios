function field(id:string,value:string){return id+String(value.length).padStart(2,'0')+value}
function crc16(payload:string){let crc=0xffff;for(let i=0;i<payload.length;i++){crc^=payload.charCodeAt(i)<<8;for(let j=0;j<8;j++)crc=(crc&0x8000)?((crc<<1)^0x1021)&0xffff:(crc<<1)&0xffff}return crc.toString(16).toUpperCase().padStart(4,'0')}
function clean(value:string,max:number){return value.normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^A-Za-z0-9 .-]/g,'').toUpperCase().slice(0,max)}
export function buildPixPayload({key,name,city,amount,txid='***'}:{key:string;name:string;city:string;amount:number;txid?:string}){if(!city.trim())throw new Error('Cidade do recebedor PIX não configurada.');const merchant=field('00','BR.GOV.BCB.PIX')+field('01',key);let payload=field('00','01')+field('26',merchant)+field('52','0000')+field('53','986')+field('54',amount.toFixed(2))+field('58','BR')+field('59',clean(name,25))+field('60',clean(city,15))+field('62',field('05',clean(txid,25)))+'6304';return payload+crc16(payload)}
