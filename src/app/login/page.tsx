'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { createClient } from '@/lib/supabase/client';
export default function LoginPage(){
 const router=useRouter();const[phone,setPhone]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');setLoading(true);try{const supabase=createClient();let d=phone.replace(/\D/g,'');if(d.startsWith('55')&&d.length>=12)d=d.slice(2);const{error:a}=await supabase.auth.signInWithPassword({phone:'+55'+d,password});if(a)throw a;router.replace('/');router.refresh()}catch{setError('Telefone ou senha inválidos.')}finally{setLoading(false)}}
 return <main className="login-page"><section className="login-card"><div className="login-brand"><div className="butterfly">♡</div><strong>Karen Martins</strong><span>COSMÉTICOS • CONSÓRCIOS</span></div><div><p className="eyebrow">ÁREA DO CLIENTE</p><h1>Bem-vinda!</h1><p className="muted">Entre com o telefone e a senha enviados pela Karen.</p></div><form onSubmit={submit} className="login-form"><label>Telefone<input type="tel" autoComplete="tel" value={phone} onChange={e=>setPhone(e.target.value)} required placeholder="(33) 99999-9999"/></label><label>Senha<input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8} placeholder="Sua senha"/></label>{error&&<p className="form-error" role="alert">{error}</p>}<button className="primary login-submit" disabled={loading}>{loading?'Entrando...':'Entrar'}</button></form><small className="muted">Ainda não recebeu seu acesso? Fale com a Karen.</small></section></main>;
}