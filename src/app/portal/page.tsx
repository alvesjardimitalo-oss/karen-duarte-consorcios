import Link from'next/link';import{BookOpen,CalendarDays,Download,Gift,ShoppingBag}from'lucide-react';import{createClient}from'@/lib/supabase/server';
const date=(v:string)=>new Date(v+'T12:00:00').toLocaleDateString('pt-BR');
export default async function PortalPage(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return <main className="client-portal"><div className="portal-wrap"><h1>Área do cliente</h1><p>Entre na sua conta para acompanhar seu consórcio.</p><Link className="primary" href="/login">Entrar</Link></div></main>;const{data:client}=await supabase.from('clients').select('id,name').eq('profile_id',user.id).maybeSingle();if(!client)return <main className="client-portal"><div className="portal-wrap"><h1>Olá!</h1><p>Seu cadastro ainda não foi vinculado à área do cliente.</p></div></main>;const[{data:members},{data:catalogs}]=await Promise.all([supabase.from('consortium_members').select('id,consortium_id,consortia(name,duration_months),installments(installment_number,due_date,status),draws:consortia(draws(draw_number,scheduled_for,winner_member_id,completed_at))').eq('client_id',client.id),supabase.from('catalogs').select('id,brand,title,cycle,view_url,download_url').eq('active',true).order('sort_order')]);const memberships=(members??[])as any[];const allInstallments=memberships.flatMap(m=>(m.installments??[]).map((i:any)=>({...i,consortium:m.consortia?.name,duration:m.consortia?.duration_months})));const next=allInstallments.filter((i:any)=>i.status!=='PAGA'&&i.status!=='ESTORNADA').sort((a:any,b:any)=>a.due_date.localeCompare(b.due_date))[0];const assigned=memberships.flatMap(m=>(m.draws?.draws??[]).filter((d:any)=>d.winner_member_id===m.id).map((d:any)=>({...d,consortium:m.consortia?.name}))).sort((a:any,b:any)=>a.scheduled_for.localeCompare(b.scheduled_for))[0];const days=next?Math.ceil((new Date(next.due_date+'T12:00:00').getTime()-Date.now())/86400000):null;const firstName=client.name.split(' ')[0];return <main className="client-portal"><div className="portal-wrap"><section className="portal-welcome"><p className="eyebrow">KAREN MARTINS COSMÉTICOS & CONSÓRCIOS</p><h1>Olá, {firstName}! 💕</h1><p>Seja bem-vinda ao seu espaço do Consórcio Karen Martins Cosméticos.</p>{assigned&&<div className="portal-highlight"><Gift size={22}/><div><span>Seu mês de contemplação</span><strong>{new Date(assigned.scheduled_for+'T12:00:00').toLocaleDateString('pt-BR',{month:'long',year:'numeric'})}</strong><small>{assigned.consortium}</small></div></div>}</section><section className="portal-grid">{next&&<article className="portal-card"><CalendarDays/><span>Próxima parcela</span><strong>Parcela {next.installment_number} de {next.duration}</strong><p>Vencimento {date(next.due_date)}</p><b>{days===0?'Vence hoje':days!<0?`Vencida há ${Math.abs(days!)} dias`:days===1?'Falta 1 dia':`Faltam ${days} dias`}</b></article>}<article className="portal-card"><ShoppingBag/><span>Pedidos</span><strong>Escolha seus produtos</strong><p>Envie código, descrição e quantidade para receber seu orçamento.</p><Link href="/portal/pedidos" className="primary">Fazer pedido</Link></article></section><section className="portal-section"><div><p className="eyebrow">CATÁLOGOS ATUAIS</p><h2>Revistas</h2><p className="muted">Consulte os ciclos atuais e envie seu pedido.</p></div><div className="catalog-grid">{(catalogs??[]).map((c:any)=><article className="catalog-card" key={c.id}><div className={`catalog-logo brand-${c.brand.toLowerCase().replace(/[^a-z0-9]/g,'')}`}>{c.brand}</div><div><strong>{c.title}</strong><span>{c.cycle??'Revista atual'}</span></div><div className="catalog-actions">{c.view_url&&<a className="secondary" href={c.view_url} target="_blank" rel="noreferrer"><BookOpen size={16}/>Ver revista</a>}{c.download_url&&<a className="secondary" href={c.download_url} target="_blank" rel="noreferrer"><Download size={16}/>Baixar</a>}<Link className="primary" href={`/portal/pedidos?marca=${encodeURIComponent(c.brand)}`}><ShoppingBag size={16}/>Fazer pedido</Link></div></article>)}</div></section></div></main>}
