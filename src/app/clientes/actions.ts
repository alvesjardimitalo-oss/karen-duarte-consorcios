'use server';
import {revalidatePath} from 'next/cache';import{redirect}from'next/navigation';import{requireStaff}from'@/modules/auth/session';import{criarCliente}from'@/modules/clientes/repository';
async function uploadAvatar(supabase:any,foto:FormDataEntryValue|null){if(!(foto instanceof File)||foto.size===0)return'';if(foto.size>5*1024*1024)throw new Error('A foto deve ter no máximo 5 MB.');if(!['image/jpeg','image/png','image/webp'].includes(foto.type))throw new Error('Formato de foto inválido.');const ext=foto.type==='image/png'?'png':foto.type==='image/webp'?'webp':'jpg',path=`${crypto.randomUUID()}.${ext}`,bytes=new Uint8Array(await foto.arrayBuffer());const{error}=await supabase.storage.from('avatars').upload(path,bytes,{upsert:false,contentType:foto.type,cacheControl:'3600'});if(error)throw new Error(`Falha ao enviar foto: ${error.message}`);return supabase.storage.from('avatars').getPublicUrl(path).data.publicUrl;}
export async function criarClienteAction(formData:FormData){const{supabase}=await requireStaff();const avatarUrl=await uploadAvatar(supabase,formData.get('foto'));await criarCliente(supabase,{nome:String(formData.get('nome')??'').trim(),telefone:String(formData.get('telefone')??'').replace(/\D/g,''),cpf:String(formData.get('cpf')??'').replace(/\D/g,''),endereco:String(formData.get('endereco')??'').trim(),tags:String(formData.get('tags')??'').split(',').map(v=>v.trim()).filter(Boolean),avatarUrl});revalidatePath('/clientes');revalidatePath('/');}
export async function atualizarClienteAction(formData:FormData){const{supabase,profile}=await requireStaff();const id=String(formData.get('id')??''),avatarUrl=await uploadAvatar(supabase,formData.get('foto')),name=String(formData.get('nome')??'').trim(),phone=String(formData.get('telefone')??'').replace(/\D/g,''),isAdmin=['SUPER_ADMIN','ADMIN'].includes(profile.role);if(name.length<2||phone.length<10)throw new Error('Nome e telefone são obrigatórios.');const{error}=await supabase.rpc('update_client',{p_id:id,p_name:name,p_phone:phone,p_cpf:String(formData.get('cpf')??'').replace(/\D/g,'')||null,p_address:String(formData.get('endereco')??'').trim()||null,p_tags:String(formData.get('tags')??'').split(',').map(v=>v.trim()).filter(Boolean),p_avatar_url:avatarUrl||null,p_active:isAdmin?formData.get('active')==='on':null});if(error)throw new Error(error.message);revalidatePath('/clientes');revalidatePath(`/clientes/${id}`);revalidatePath('/');redirect(`/clientes/${id}`);}
